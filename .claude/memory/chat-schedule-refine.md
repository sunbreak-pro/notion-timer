# MEMORY (chat-schedule-refine)

## 進行中

### 🔧 担当キューの消化（着手日: 2026-08-01）

**対象**: `web/src/schedule/` / `shared/src/components/schedule/` / `shared/src/hooks/` / `shared/src/utils/`

- 前回: **担当キューを空にした**（2026-08-18・残り 9 件を 1 Issue = 1 ブランチ = 1 PR で提出）。#996=#1080 / #1033=#1081 / #1034=#1082 / #995=#1085 / #1044=#1088 / #998=#1090 / #997=#1092 / #889=#1094 / #1000=#1095。**提出時点で #1080 #1081 #1082 #1085 は merge 済み・残り 5 本 open**。全ブランチ origin/main から独立に切り、ローカルで CI `verify` 全ステップ + `docs-lint` = exit 0
- 旧: **判断キュー 2 件の昇格（PR #990）+ #889 の 3 ユニット（#1016 / #1018 / #1022）= すべて merge 済み**（2026-08-16 深夜〜17 未明）
- 旧: **監査由来 3 件 + #940 + #889 の 4 ユニット = PR 8 本すべて merge 済み**（2026-08-16 夜。#932 = #964 / #933 = #966 / #934 = #968 / #940 = #972 / #889 = #974・#976・#982・#987。決定台帳 = #959 / tracker = #989）
- 旧: **#889 = PR #941 merge 済み**（2026-08-16・main `8dd6691d`）。右サイドバー 3 タブを `web/src/schedule/ScheduleSidebar.tsx` へ
- 現在: **/goal 6 件（#1371 / #1403 / #1440 / #1405 / #1406 / #1401）を提出し終えた**（2026-09-02）。**#1450 / #1452 / #1454 / #1458 は merge 済み**（2026-09-02 13:44Z 前後）、#1463 / #1464 は open。**main が `web — lint` で赤になった**（#1456 = #1375 の `workTime` と #1458 = #1405 の `convert` が隣接し、merge で `workTime={workTime}` の 1 行が落ちた）→ **fix PR #1466** を origin/main から切って復元（整形漏れ 3 ファイルの prettier も同梱）。全ブランチ origin/main から独立に切り、CI `verify` 全ステップ + `docs-lint` をローカルで exit 0（#1403 の web vitest だけ既知の flaky = `briefingEveningLazyMount` が負荷で落ち、単体緑で通した）。**判断 2 件は 2026-09-05 に回答済み・台帳へ昇格済み**（D-20260902-sched-1 = A / D-20260902-sched-2 = C。どちらも実装済みの案なので追加 PR 無し）。**open 3 本の CI が赤だったのは main の `workTime` lint（#1466 で解消）を merge 結果で拾っていたため** — 3 本に origin/main を取り込んで push し直した。レポート = `.claude/docs/reports/2026-09-02-schedule-six-prs.html`（Artifact: https://claude.ai/code/artifact/0b1c8b80-a55c-4cd9-b10a-d3c5e57ac89c）
- 旧: **/goal 5 件（#1362 / #1370 / #1367 / #1373 / #1374）を提出し終えた**（2026-09-01）。**#1411（#1362）/ #1413（#1370）/ #1414（#1367）は merge 済み**、**#1424（#1373）は origin/main 取り込みのコンフリクト解消まで済んで `MERGEABLE/CLEAN`**、**#1433（#1374）は open で 🛑 `supabase db push` 待ち**（`supabase/migrations/0027_events_payload_reminder_offset.sql`。新列が `EVENTS_PAYLOAD_COLUMNS` の SELECT 一覧に入るので **push が merge より先**でないと schedule の SELECT が全部 PostgREST 42703 で落ちる）。5 本すべてローカルで CI `verify` 全ステップ + `docs-lint` = exit 0
- 旧: **#1343 = PR #1355**（2026-08-31・merge 済み）。予定の詳細パネルのタブ 3 つのうち「今日の流れ」「本日の Todo」だけがラベル途中で折れていた件。既定 320px のパネルはボディの `p-3` を引いた残りを 3 分割するので、**既定ルートフォント 18px ではラベル 1 つ約 67px しか無く**、ja も en も長い 2 つが入らない（「繰り返し」= 約 63px だけ入る）。`SegmentedControl` に opt-in の `singleLineLabels` を足し、ラベルを `whitespace-nowrap`・トラックを `flex-wrap`（**折るのは語ではなく行** = ツアーフッター #1264 と同じ手）にしたうえで、左右パディングと文字サイズを 1 段返して（`px-3 text-sm` → `px-1.5 text-xs`）320px に 3 つを収めた。ローカルで CI `verify` 全ステップ + `docs-lint` = exit 0（shared 281 files 2,773 / web 106 files 990 / desktop 1 files 7 / mcp-server 25 files 322）
- 旧: **#1242 = PR #1267 が open**（2026-08-30）。Schedule ツールバーのタグフィルタが `aria-label` で "Filtered by 1 tags" と読み上げられる件を、`scheduleScreen.filterActive` の `_one` / `_other` 分割で解消。en は 2 形・ja は `_other` のみ（既存 `todoCount_other` と同じ）で、呼び出し側（`web/src/schedule/scheduleCopy.ts:211`）は `count` を既に渡しているので無変更。ローカルで CI `verify` 全ステップ + `docs-lint` = exit 0（shared 274 files 2,677 / web 100 files 937 / desktop 1 files 7 / mcp-server 24 files 319）
- 旧: **#1173 = PR #1226（GitHub CI 両ジョブ pass）/ #1207 = PR #1233（CI 実行中）が open**（2026-08-29）。#1173 は Calendar ツールバーの歯車をフィルタアイコンに差し替え、既存 WikiTag のマルチセレクト + 保存グループでカレンダー台帳を完全代替。**DDL ゼロ** — `wiki_tag_groups` / `wiki_tag_group_assignments`（0008 で作られ RLS も publication も済み・書き手ゼロ・本番 0 行）を流用したので、merge した瞬間から動く。`public.calendars` は退役-in-place（DROP は 🛑 push ゲートで別 PR。依頼は outbox）。#1207 は `SegmentedControl` のセグメントを flex 中央寄せにして、折り返したラベルと 1 行ラベルの縦位置を揃えた
- 旧: **5 本が merge 待ち**（#1088 / #1090 / #1092 / #1094 / #1095）+ **判断キュー 3 件が回答待ち**（D-20260818-sched-1 = #997 の Undo が破棄フィールドまで戻すか / -2 = #1033 で narrow の Todo タブが空ドロワーを開く件 / -3 = #1000「Todo チップ」が日リストの行か月グリッドのドットか）。`CalendarTab.tsx` は #1094 適用後で **1,479 行**（開始 2,234 から -755）。1,000 行まであと約 480 行
- **push-after-merge を 4 度目に踏んだ**（2026-09-02）: #1452 / #1454 の整形 commit を push した時点で両 PR は merge 済みで、commit はブランチに取り残された（memory `push-after-merge-strands-commits` そのもの）。**PR を立てたら以後そのブランチには push しない**を整形 commit にも適用する — 直すなら origin/main から新ブランチ（= #1466 の形）
- **隣接ハンクの squash 連続 merge で片方の行が消える**（2026-09-02 実測・#1456 → #1458）: 別レーンが同じ JSX の隣の prop を足し、自分が隣の prop を書き換えると、GitHub 側の merge で相手の 1 行が落ちて main だけが赤くなる（lint の unused-var）。**merge 直後に `origin/main` で lint を 1 回回す**か、PR 巡回で「同じファイルを触る open PR」を先に見る
- **python で当てたパッチは整形フックを通らない**（2026-09-02 実測）: Write / Edit ツールなら PostToolUse の prettier が走るが、スクリプトでファイルを書くと素通りし、push 後の `prettier --check` で 7 ファイルが未整形だった。**スクリプトで書いたら commit 前に `npx prettier --write` を自分で当てる**。CI は prettier を見ないので気付くのは人の目だけ
- **`(cmd) > "$S/logs/$br.log"` はブランチ名のスラッシュで死ぬ**（2026-09-02 実測）: `claude/xxx` を含むログパスはディレクトリが無く、リダイレクトの失敗が exit 1 になって**コマンド自体が走らない**。結果を gate に使っていないと未検証のまま push してしまう（実際に 3 本を追検証で救った）。ログ名は `${br##*/}` で刻む
- **#1000 は「作り直さない」が正解だった**（2026-08-18 実測）: Issue 本文は 2026-08-10 の outbox 依頼の写しで、chat-main の起票（08-16）までに **#761 が #626 の上に narrow のシートを載せて着地済み**だった。2 つ目の面を作ると `todoDetailId` を奪い合い、どちらも未保存ガードのコピーを持つ = `ScheduleTodoDetail` が防ぐために書かれた重複そのもの。**Issue 本文が古いことがあるので、着手前に「もう有るのでは」を必ず確認する**
- **「同一の部品を使う」は grep して確かめる**（#1034）: Materials の「+ノート」は部品ではなく**インラインの `<button>` が 2 コピー**（6 トークンの同じクラス文字列がバイト単位で同一）だった。3 つ目のコピーを足すのは DoD の「同一の部品」ではないので、`shared/src/components/AddPill.tsx` に切り出して 3 箇所とも差し替えた
- **ロール表示はパネルではなくフレームが持っていた**（#1044）: 「予定」は `EventEditorPane` の origin チップ、「Todo」は **TagPicker がタグ行のキャプションとして描いていた kind バッジ**で、そもそも**どちらのパネルもヘッダーを持っていない**（タイトルは `Modal` / `BottomSheet` 側）。「ヘッダー付近へ移す」= フレーム連鎖（`ResponsiveDetailFrame` → `ItemDetailOverlay` → `Modal`、および `BottomSheet`）に `titleIcon` スロットを足す、が実装の形になる
- **グリフだけにすると a11y が退行する**（#1044）: `ItemRoleBadge` の `aria-label` は素の `<span>` に乗っていて `generic` ロールでは名前が確実に露出しない。**言葉が消えた面でこれを放置すると「唯一の手がかりが飾りになる」= 名前の付け替えではなく退行**。`role="img"` を足して初めて `getByRole("img", { name })` で assert もできるようになる
- **抽出で `useState` セッタを引数にすると eslint の暗黙知が切れる**（#889）: コンポーネント内なら安定と知っている `setSelectedId` が、引数になると依存配列の欠落として警告される。**足すのは実行時 no-op** なので足す（入れない選択肢は新規 warning 2 件）。挙動保存の証明は「移動後のコードを rename だけ当てて `git show HEAD:` と diff」で機械的に取れる
- **Undo は「逆変換」だけでは足りない**（#997）: 変換はどちらの方向も新しい payload を古い行から組み立てて**自分を完全に指定した行を UPSERT する**ので、組み立て側が触れていない列は NULL / false で返る。逆変換だけ走らせると**種類は正しいが形が違う行**に着地する（親リンク消失・優先度クリア・dismissed 復活）。「逆変換 + スナップショットのパッチ」がセットで、パッチのキー集合はリテラルでテストに固定する（フィールドを足したとき考える唯一の瞬間を作るため）
- **フル verify は 1 本あたり約 7.5 分**（2026-08-18 実測・warm cache）。9 本で約 70 分。**`| grep` でフィルタすると `set -e` の失敗が exit 0 に化ける**ので、ステップごとに走らせて失敗時だけ tail を出すランナーにする（1 度これで `shared test` の赤を見落としかけた）
- **web の重い suite は負荷で flaky になる**（2026-08-18 実測）: `briefingEveningSavedCaption` の TipTap マウント待ちが並列実行下で timeout した。単独では通るので、落ちたら**まず単体で再実行して切り分ける**
- **`min-w-0` が無い flex 列は「幅が広すぎる」ではなく「縮めない」で溢れる**（#1036 の原因・narrow 全般に効く）: flex アイテムの `min-width` は既定 `auto` = **中身の min-content 幅が床**になり、素の `<input>` はブラウザ既定の約 20 文字分（Chrome で概ね 180px）を申告する。`flex-1` を 2 つ並べれば床だけで 370px 前後になり、375px では**右へ抜ける以外に解決しようがない**。`w-full` は効かない（親が intrinsic minimum を計算する間、パーセント幅は無視される）。**`<input type="date">` + `shrink-0` のボタンという組み合わせも同型**で、これが「終日ボタンと重なる」の正体だった
- **レイアウト崩れは jsdom で測れないので「クラスの綴り」を固定する**（#1036）: 寸法が全部 0 なので溢れ自体は検証不能。ただし今回の退行は**ユーティリティが書かれていなかった**ことなので、`cn`（tailwind-merge ではなくただの文字列連結）で無言に落ちる 1 クラスを見張るのは筋が通る。**片側だけ固定しない** — 日付列が縮めるようになったことに意味があるのは隣が `shrink-0` だからで、両方 assert して初めて守りになる（先例 = `bottomSheetSafeArea.test.tsx`）
- **担当外の mcp-server を直して main の赤を解除した**（PR #1031・こうだいさん確認のうえ着手）: `searchAll` の戻り値が `{ totalHits: number }` だけになっていた。`return { ...result, totalHits }` で、`number` が index signature（`DomainPage<unknown>`）に代入できないため **TS が signature を広げるのではなく丸ごと落とす** → ドメインのキーが全部消える。**実行時は正しく `.todos` を返しており、忘れているのは型だけ**。支えが 2 本（既存テストの `as Record<string, unknown>` 迂回 / mcp-server に `typecheck:tests` ゲートが無い）あって、**#1010 がゲートを足し #1003 が `.todos` を素直に読むテストを足した瞬間に両方外れた** — 単独では両方緑の意味的な合流事故
- 旧: **判断キュー 2 件を台帳へ昇格（PR #990）+ #889 を 3 ユニット追加提出**。D-20260816-sched-2 = A（テストはファクトリだけ畳む）/ D-20260816-sched-3 = A（一括生成の R2 cleanup は現状維持）
- **#1016 は #889 の副産物として実バグを見つけた**: 2 つのレイアウト分岐が**それぞれ手でオーバーレイを数え上げていて、Desktop 側にだけ `<ConfirmDialog>` が無かった**。`useConfirmDialog().ask()` は「その dialog が答えたとき」にしか解決しないので、Desktop では**確認が永久に返らない** = 編集途中の詳細が閉じられない（リロード以外に出口が無い）／ 子持ち Todo の削除が走らない／ Event↔Todo 変換が止まる。**例外もトーストも出ないので 8 ゲート全部が緑のまま通る**。直し方は「Desktop に 1 個足す」ではなく「両分岐から数え上げること自体を無くす」
- **CalendarTab.tsx の行数はユニット単位では大きく減らない**（実測）: 2,234 →（#1016）2,054 /（#1018）2,075 /（#1022）2,117。**1 ユニットあたり 110〜180 行**で、1,000 行に届かせるにはこの規模があと 6 本以上要る。**同じホストを触る PR を貯めると衝突管理のコストが上がる**ので、区切りごとに merge してもらってから次を切る
- **#982 は CI が一度も発火しなかった**（2026-08-16 実測）: push 直後も、空コミットを積んでも、close → reopen しても `gh run list --branch` がゼロ件。**origin/main を取り込んで push した時に初めて走った**（他の全ブランチは初回 push で普通に発火していたので GitHub 側の取りこぼしと見る）。**`gh pr checks` が「pending」ではなく「no checks reported」を返したら、走っていないのであって遅いのではない** — 待たずに何か push する
- **push-after-merge をまた踏んだ**（memory `push-after-merge-strands-commits` の 3 度目）: PR #959 が 07:42Z に merge され、その後に積んだ tracker commit `8900e0d7` はブランチにだけ残った。`chore/tracker-schedule-refine-20260816-5` を origin/main から切って cherry-pick で回収。**tracker ブランチも「PR を立てたら以後 push しない」**（実装ブランチと同じ規律）— tracker を後から足すなら新しいブランチを切る
- **打ち切り表示は「上限が 2 つある」ことを疑う**（#1045）: 月グリッドはチップ 2 個 / ドット 3 個で切るのに `overflow` が 1 つしか無かった。共有すると**ドット行の下にチップ側の数字**が出る（8 件の日が「+5」ではなく「+6」）。テストは**マーカーの有無ではなく数字そのもの**を固定する — 存在チェックだと間違った上限の値も通る
- 旧: **#1098 と #889 を出し切った**（2026-08-24）。#1098 = PR #1113 **merge 済み**（main `53ed85b0`）+ 追いかけの精度修正 PR #1132 open / #889 = PR #1131 open で **`CalendarTab.tsx` が 1,538 → 983 行**（DoD の 1,000 行を達成）。3 本とも CI `verify` 全ステップ + `docs-lint` をローカルで exit 0
- **サブエージェントが repo を書き換えて commit を reset した**（2026-08-24・最重要）: #1098 のレビュー中、**セッション中断を跨いで生きていた別ワークフローの implement エージェント**が `deleteScheduleItem` を検証用の形（`const q = …from("items_meta");` + `q.delete().eq("id", id)`）に書き換え、さらに `git reset HEAD~1` を打っていた。その細工が最初の push に載った。**犯人の特定を自己申告に頼らない** — 停止させた role-qa は「書いていない」と答えたが、`git reflog` の `reset: moving to HEAD~1` と push 済み blob が動かぬ証拠だった。**教訓 2 つ**: ① 長時間ワークフローはセッションを跨いで生き残るので、`git add` の直前に `git status` を撃つ ② エージェントのプロンプトに「state-changing git 禁止・実験は scratchpad で」を明記する（以後のワークフローには全部入れた）
- **census の自己チェックが細工を捕まえた**（2026-08-24）: 上の `const q` 形は**チェーンが分断されて walker から見えなくなる**が、`.delete(` の生カウントと突き合わせていたので 5 対 4 の不一致で落ちた。**代理指標より「`.from("items_meta")` が動詞に到達するか」を直接見るほうが良い**（PR #1132 で差し替え）— 生カウント方式は `from("events_payload").delete()` や `claimed.delete(key)` で誤爆する（両方実測）
- **レビューの「新しい prop を足している」を鵜呑みにしない**（2026-08-24）: #889 のレビューが `weekStartsOn` を behaviour change と報告したが、**待っている間に main へ #1102（PR #1126）が入り週初め設定を配線ごと退役させていた**のが真相。自分の差分は退役前の main から切って元の形を運んでいただけ。**レビュー結果が「追加した」と言うときは、比較相手の main が動いていないかを `git rev-parse origin/main` で確かめる**
- **新規テストの「不安定」は書き方の問題**（2026-08-24）: `ScheduleOverlayHost` の 4 ケースが `askConfirm`（**同期で呼ばれる中間**）を待って、`clearDirty` と `close()` が 2 マイクロタスク後に来る前に次のアサーションへ進んでいた。ゲートを 1 回赤にした。**待つのは終端の効果**（`onCloseOverlay` / `onConvertToTodo`）で、否定アサーションの前は `await act(async () => {})` でチェーンを流す
- 旧: **/goal の 4 件を全部 PR まで出した**（2026-08-29）。#1140 = PR #1163 **merge 済み** / #1124 = PR #1168 open / #1148 = PR #1178 open / #1153 = PR #1187 open（計画書 → 実装の 2 commit）。4 本とも origin/main から独立に切り、CI `verify` 全ステップ + `docs-lint` をローカルで exit 0
- **Issue 本文が予告していた修正が、コメントに先に書いてあった**（#1140）: `permanentDeleteRoutine` の doc が「bump の行数を読め」「step 2 が `.select("id")` すべき」と**やるべきことを名指しで残して DEFERRED していた**。読めば設計は済んでいる。**この repo では「直し方は既にコメントにある」を最初に疑う**
- **アンカーは「ステップが始まった時点で画面にある物」しか指せない**（#1124・ツアー基盤の制約）: Provider は要素を探して見つからなければそのステップを飛ばすので、**そのステップ自身がユーザーに開かせようとしている物**（編集パネルの中の時刻欄）は指せない。指すのは durable な面（カレンダー / ボード）にして、前進は `advanceOn` の書き込みで待つ
- **通知はホスト側で包む**（#1124）: `useScheduleMutations` / `useScheduleCreateFlow` は Provider 無しで `renderHook` に載るよう意図的に context-free に作られている。中から Context を触るとその性質が消えるので、CalendarTab / KanbanView 側でハンドラを包んだ
- **`useTourAction` を optional かつ identity 固定にした**（#1124）: 保存処理はツアーが聞いているかを気にしてはいけない（= ツアー無効時に挙動が変わらない担保）し、ツアーが 1 歩進むたびに包んだハンドラが作り直されるのも避けたい。ref 越しに読む形
- **`#878` を覆すときはコメントに新旧の両方を残す**（#1148）: 「ドットは WHERE、下のリストは WHAT」という #878 の読み自体は今も正しく、崩れたのは算数のほう（隣のドロワーが同型のリストを出していたので同じ形が 2 枚あり、月が 1/3 に押し込まれていた）。新しい答えだけ書くと、次に読む人が「なぜ 2 枚だったのか」を再発明する
- **`web typecheck:tests` だけが赤くなる罠を 1 回踏んだ**（#1148）: テストヘルパの `day?` オーバーライド型が残っていて、vitest は型を見ないので**他のゲートは全部緑のまま**だった。CLAUDE.md §7.1 が名指ししている形そのもの
- **計画を書いたら作業の中身が変わった**（#1153）: 調査で「トレイが既に未スケジュール一覧（`pickAddableTodos`）と今日への配置導線を持つ」「Schedule に既に Todo 詳細面がある（本文だけ無い）」が分かり、**新規実装ではなく差し替えと撤去**になった。差分 -4,600 行。**Issue の「〜を統合する」は、実装済みの部品を数える前に読まない**
- **退役で 2 つだけ板より長生きした**（#1153）: `Kanban/colors.ts`（`ColorPicker` がタグ色に使用 → `components/colorPresets.ts` へ）と、`useTodoLinking` がコンソール用に渡していたホスト名 `"KanbanView"`（**存在しないファイルを指す名前になる**。`useInlineItemLinks` が `hostTag` を引数に取っているのはまさにこれを避けるため）
- **effect 内の同期 setState は素直に諦める**（#1153）: 作成ダイアログの開閉を effect に置いたら `react-hooks/set-state-in-effect` が正しく落とした。退役した `useTodoAddDialog` が同じフラグに使っていた「描画中に state を調整する」React 公式パターンをそのまま持ち越した
  - 現在: **#1168 が merge され、予告どおり #1187 側で 2 巡目をやった**（2026-08-29）。6 ファイル衝突（4 件は modify/delete = 板の 3 ファイル + `kanbanView.test.tsx`）を解消し、**#1124 のツアーアンカー 3 本をサイドバーへ付け替えて push**（`b2753770`・MERGEABLE + CLEAN）。#1140 / #1148 / #1168 は main 着地済みで、**残る open は #1187 の 1 本だけ**（merge はこうだいさんの手番 = P-001）
- 旧: **#1168 / #1187 のコンフリクトを解消して push**（2026-08-29）。両方 MERGEABLE + CLEAN に復帰（#1168 = `8bc140fb`・CI も緑 / #1187 = `c1fc55b7`）。**#1168 を先に merge するのが素直** — 後着地側が #1124 のツアーアンカー 3 本を新しい面へ付け替える約束（`2026-08-29-schedule-todo-tab-retirement.md` L100）なので、その場合 #1187 側で main を取り込み直す 1 往復が要る
- **面を退役させる PR は「文言」まで追いかける**（2026-08-29・#1153 x #1124）: ツアーの `scheduleCompleteTodo` が「カードを完了列へドラッグ」と、消した板の操作をそのまま教えていた（`scheduleOpenTodos` の「Todo シート」も同型）。**アンカーとハンドラを付け替えるとゲートは全部緑になるので、文言だけが取り残される**。退役 PR では i18n catalog も grep する
- **「画面に出ている」の判定はホストの state ではなく描画の事実で取る**（2026-08-29・#1153）: 「Todo を開いた」の報告をホスト（`CalendarTab` のタブ id）に置くと、パネルを閉じていてもタブ id は残るので**誰にも見えていないトレイ**を報告してステップが先に進む。`RightSidebarPortal` は閉じている間 children を描かないので、**サイドバー側の effect にすれば「生きている = 見えている」が同義**になる
- **消える面のテストは「行き先」を先に決める**（2026-08-29・#1153）: 板と一緒に `kanbanView.test.tsx` が消え、#1124 のツアー 3 件も道連れになった。**サイドバー側は実描画・`CalendarTab` 側はソーステキスト assertion**（jsdom に載らない面の逃げ道 = `rules/frontend.md`・先例 `scheduleNarrowAdd.test.ts`）に分けて `web/tests/scheduleTourTodos.test.tsx` へ移した。ツアーの progress は localStorage に残るので、ケース間で `localStorage.clear()` が要る（1 ケース目が完走すると 2 ケース目が起動しない）
- **コンフリクト解消の途中で main が動く**（2026-08-29・#1168）: 開始時に fetch した `origin/main` で 4 件を解決し切ったあと、別 PR を `git merge-tree` で調べたら **main が 9 コミット進んでいた**。古いまま押していたら #1148 との衝突（narrow の作成ピルが日別リストごと消える）を取りこぼし、スマホ幅でツアー 1 歩目が無言で飛ぶところだった。**解決に入る直前にもう一度 `git fetch` して `git rev-parse origin/main` を突き合わせる**
- **並行 PR は「同じ意図の実装」を 2 本作る**（2026-08-29・#1168）: main が `useTourContextOptional` を足している間に、こちらは `useTourAction` の中で同じ optional context hook を自作していた。**マーカーに出ない衝突**なので、マージ後に「両側が同時期に足したもの」を grep して畳む
- **文字距離を見張るテストはコメントで落ちる**（2026-08-29・#1168）: `scheduleNarrowAdd.test.ts` が「日付キャプションと `<AddPill>` の間 400 文字以内」で近接を固定しているので、ピルの直前に置いた 5 行コメントで赤くなった。**説明は見出し行の既存コメント塊へ寄せる**
  \=======

> > > > > > > origin/main

- 次: **#889 の残り**（2026-08-17 こうだいさん指名 — **2026-08-24 に PR #1131 で完了**）。#1094 で作成パネル（約 157 行）が抜けたので、残る候補は **Derived data ブロック**（約 210 行）/ **Mobile 本体の JSX**（#987 が Desktop にやったのと同じ hoist・約 130 行）/ **`editorPane` + `editorLabels`**（約 80 行）。**3 つ全部やっても約 420 行 = 1,060 行前後**なので、1,000 行を切るにはもう一段（Todo 面の残り or `useCalendarNav` 周辺）が要る。`Closes #889` は 1,000 行を切る PR にだけ付ける
- **次の抽出は #1088 / #1090 の着地を待つ**（2026-08-18 判断）: 残り候補のうち JSX フラグメント群は `editorPane` 周辺で、そこは #1044（`titleIcon`）と #998（`convert`）がまさに触っている領域。先に merge してもらってから切る方が衝突管理が安い
- **#889 の抽出先は `shared/` ではなく `web/src/schedule/`**（2026-08-16 判断・PR #941 本文に記載）: Issue 本文は `shared/src/components/schedule/` と書いているが、切り出す対象は **shared に既にいる部品を組み合わせるだけの層**で、自分の文言は `useTranslation()` で解決している。shared へ押し込むと組み合わせ以外に何もしない層に約 25 個のラベルを通すことになり、#893 でその下の部品から取り除いた形そのものになる。#675 が剥がした 15 ファイルも全部 `web/src/schedule/` にいる
- **ポータルは切り出し先に畳み込まない**（#889 の実測）: `RightSidebarPortal` は Provider 無しだと `null` を返す（`RightSidebarPortal.tsx:30`）ので、部品側に入れると**シェル一式を立てないテストから中の分岐が 1 つも見えない**。配置はホストの都合として呼び出し側に残す
- **変異テストの当たり形**（#889 で実践・#897 の反省を反映）: 狙った規則 3 つを同時に潰したら**落ちたのはちょうど対応する 3 テストだけ**（8 件は緑）。件数ではなく「どれが落ちるか」が 1 対 1 で対応しているかを見る
- **#893 で DoD の「既存テストが無改変で緑」は文字通りには満たせない**（2026-08-16 実測）: props の形を変える以上、`<WeekTimeGrid weekStart={...}>` と書いてある行はテストでも書き換わる。代わりに**次に強い形**を作った = **`it(...)` の中身・アサーション・操作手順は 1 行も変えず**、各テストファイル冒頭の render ファクトリだけが束ね直しを引き受ける（`renderPane(manualItem, { canEditDate: true })` のようなフラットな呼び出しはそのまま）。**ファクトリの `{...props}` スプレッドは明示マージに変える必要がある** — スプレッドは束ごと置き換わるので `{ existingTodos: [] }` がノート側のプールまで巻き添えで消す
- **#893 の着手前に効いた実測**: `ItemCreatePanel` のホストは 3 つある（`CalendarTab.tsx:2195` / `QuickCaptureSheet.tsx:60` / `BriefingScreen.tsx:477`）。**Briefing は Schedule のセクション層 Provider の外**なので、Issue 本文の「セクション層 Context から直接取れるものは props を廃止する」を当てると Briefing が壊れる。オブジェクト prop への束ね側だけで畳んだ（Context 消費はゼロ = ステップ 2 は不採用）
- **束ねると型が守ってくれる 2 箇所があった**（#893 の副産物・ここが本命の収穫）: ① `EventEditorPane` の繰り返しセクションは `repeatLabels` + `repeatWeekdayLabels` + `onChangeRepeat` の**3 つの optional prop が全部揃ったときだけ**描画されていた（2 つだけ渡すと無言で何も出ない）→ 必須メンバーを持つ 1 オブジェクトにして半端な配線をコンパイルエラーに ② `ItemCreatePanel` の 4 つの書き込みは 1 つの能力で、3 つだけ配線すると「4 つ目のタブで押しても何も起きない送信ボタン」が出荷できた
- **#893 ステップ 3 の実測（`MonthGrid` / `AgendaList` / `TodayTodoTray` と `WeekTimeGrid` の描画重複）**: **1:1 の重複は 1 件だけ** = `dotColorClasses` が MonthGrid と AgendaList に**バイト単位で同一**の 9 行 → `scheduleVariantVisuals.ts` に集約（`variant` のユニオンも 3 つの item 型が別々に書いていたので同居させた）。**face マッピングは寄せない** — `variantBlockClasses`（WeekTimeGrid）と `chipFaceClasses`（MonthGrid）は同じ switch に見えて解決先のトークン族が違う（`schedule-*-bg` vs `chip-*-bg`。ブロックとチップは読まれるサイズが違うため意図的に別）。統合には族を選ぶ引数が要り、それは「関数 2 つ + 手間」と同義。`TodayTodoTray` は共有する描画が無い（Todo 面の語彙）
- **#889 の現状**: `web/src/schedule/CalendarTab.tsx` は 2,567 行で、トップレベルは定数 4 本 + `CalendarTab` 関数 1 本だけ（`grep "^\(export \)\?\(function\|const\|interface\|type\) "` で実測）。#675 で既に 11 ファイルが切り出し済み（`useCalendarNav` / `useScheduleMutations` / `useVisibleRangeItems` / `todoChipPanel` / `todoChipUndoWiring` / `unsavedCloseGuard` 等）なので、残りは**関数本体の中**にある
- **narrow の実測は「390px の同一オリジン iframe」で取れる**（2026-08-11 実測）: `resize_window` は OS 側で効かず `innerWidth` が 2560 のままだったが、iframe の中では `matchMedia("(min-width: 768px)")` が false になり Mobile 分岐が描かれる。§7.4 の「dev server は chat-main のみ」は**起動の話**なので、既に立っている 5173 を読むだけなら worktree からでも実測できる
- **#691 の実測サマリ**: 行高は所要時間に依らず一律 43px（180 分も 60 分も同じ）／ 終了時刻は `AgendaItem.endTime` として渡っているのに描画されない（`AgendaList.tsx:24` vs `:137-139`）／ 空き 60 分でも隙間 0px ／ **進行中の予定が現在線の上（過去側）に出る**（分割が開始時刻だけを見る = `AgendaList.tsx:100-102`）／ 予定 0 件の日は今日でも現在線が出ない（線が timed の map の内側にしかない）／ **#593 の Todo アクセントは narrow で無傷**（ドット rgb(91,140,255) + CheckSquare・幅分岐が構造的に無い）／ ただし Todo 行は status 無しで完了不可（`CalendarTab.tsx:1188-1196`）かつタップ no-op（`:471` が `if (isWide)`）
- **#692 は MonthGrid の作り替えではなく配線で足りる**（2026-08-11 実測）: `MonthGrid` は narrow 用 `compact` モードを実装済み（`MonthGrid.tsx:62-63` + `shared/tests/monthGrid.test.tsx:73-110`）だが `CalendarTab` から一度も渡されていない。セルは `min-h-14` = 56px でタップ標的として足りる
- **`effView` を直す箇所は `useCalendarNav.ts:32` の 1 行だけ**（2026-08-11 トレース）: `step()` の month 分岐（`:64`）も `visibleCalendarRange` の month 分岐（`shared/src/utils/calendarView.ts:44`）も `periodLabel`（`CalendarTab.tsx:918`）も **`isWide` で囲われていない**ので、narrow で `effView` が `"month"` になれば移動単位・取得範囲・ラベルが自動で追従する。残る配線は「narrow の描画分岐に MonthGrid を出す」1 点（wide の month 分岐は `effView` ではなく `desktopView` を読む = `:2375`）
- **#503 と #467 は `CalendarTab` で真正コンフリクトした**（2026-08-01 実測）: #503 のパレット着地 effect が `setMobileSelectedDay` を呼ぶのに対し、#467 は Mobile の月アジェンダごとその state を退役させた。**片方が消した API をもう片方が使う形**なので union では直らず、アンカー移動 1 本に畳んだ（Mobile のリストはアンカー日を読むので追随する）
- **squash merge は tracker のエントリを落とすことがある**（2026-08-01 実測）: #503（PR #513）/ #505（PR #515）は**コードは main にあるのに history のエントリだけ消えていた**。旧ブランチの commit（`9185969a` / `fb960baa`）から該当ブロックを取り出して復元した。**merge 後は「コードが着いたか」と「記録が着いたか」を別々に確かめる**（`git show origin/main:<path>` で実在確認）
- **#506 の Important は「片側だけ直した」典型**: `setCalendarFilterId(null)` が 4 経路中 `handleCreateSubmit` にしか無く、タスク作成 / 既存タスク配置 / 作成して開く の 3 経路が素通りしていた。**タスクチップも同じ `applyCalendarLens` を通る**ので症状は完全に同じ。4 経路が通る `finishCreatePanel()` に合流させて、次に増える経路が同じ抜け方をできない形にした。**cancel 側（`onClose`）は合流させない** — グリッドに何も増えていないのにユーザーのレンズを外すのは別の壊れ方
- **#514 は「無言の場所が 1 段ずれただけ」だった**: 順序を直した結果、今度は `propagate`（occurrences への反映）が唯一 verdict の無いステップとして残り、call site の素の `catch` が飲んでいた。#504 の題目そのものなので別 Issue に切らずこの PR で拾った。`propagate` を `Promise<boolean>` に揃え `"propagate-failed"` を追加。**`updateFutureOccurrences` は throw で失敗を返す**ので、その場で `false` に変換して外側 `catch` に落とさない（外側は「テンプレが通ったか」を区別できない）。文言は `series` と分けた = `series` は「予定は変更されていません」と言い切る文で、テンプレ保存済みの状態で出すと嘘になる
- **`handleScopeChoose` の依存配列に `onRepeatConvertFailed` が抜けていた**（#504 のコミットでこのコールバックから使い始めたのに未宣言）。web lint の warning 1 件がこれで、同じコミットで解消 → **web lint は warning 0**
- **rollback ではなく順序で解いた**: 旧実装は occurrence を書いてから template を await せずに撃つ順序で、template が落ちると**画面は完全に正しいのに template だけ古い**という、リロードでも検知できない食い違いが残った。**template を先に書いて落ちたら中断**すれば、失敗時点で occurrence に触れていないので「何も保存されていません」が嘘にならない
- **`fillUpToAnchor` は引き続き最初**（アンカーより前の日 = ユーザーが選ばなかった日は pre-edit の値を保つ必要がある）
- **文言分岐はネスト三項からテーブルへ**。reason は増える一方で、チェーンだと新しい reason が黙って最後の `else` に落ちる（「何も保存されていない」が「変更できました」に化ける）

### 🔧 Epic #290 の残 4 件（#466 → #469 → #468 → #467）（着手日: 2026-07-30）

**対象**: `web/src/schedule/` / `shared/src/components/schedule/` / `shared/src/utils/` / `shared/src/i18n/locales/`
**計画書**: `.claude/docs/vision/plans/2026-07-14-schedule-redesign.md` §6 Step 5-b / 6 / 5-c / 7

- 前回: #469 の role-qa 監査対応（hardening follow-up）まで着地。#466（PR #480・main `9ff4a813`）と #469（PR #484・main `7c4bd530`）はどちらも merge 済み・Issue も close 済み
- 現在: **#467 Step 5-c Mobile List+FAB = branch `claude/schedule-467-mobile-list-fab`**（origin/main `37691157` から）。実装完了・7 ゲート全緑（shared 166 files 1386 pass / web 9 files 79 pass / docs-lint OK）。**Epic #290 の担当 4 件はこれで全部着地**（#468 = PR #506 が 7/31 merge 済み）
- **#467 で一番踏みやすかった罠 = `view` state はレイアウトを跨いで残る**: mobile の switcher を消しても `view` は Desktop の選択（"month" 等）を持ち続けるので、**render 分岐だけを list に固定すると `step` が月送りのまま・フェッチ窓も月グリッド全体**になる。固定は `useCalendarNav` 側（`effView = isWide ? desktopView : "list"`）に置いて、描画・ナビ・フェッチ窓が同じ 1 つの事実を見る形にした。`normalizeMobileView` は shared ごと退役したが、**退役 id（"time" / "list"）は `normalizeDesktopView` が吸収し続ける**（長命な state に残っている可能性があるため）
- **閲覧のみは `readOnly` フラグではなく「コールバックを渡さない」で表現する**（#467 の `RepeatListPanel`）: フラグを足すと「削除を出すか」の真実が 2 つになり、いずれ食い違う。disabled にもしない — 押せるのに断るコントロールは壊れて見える（#434 S-1 と同じ原則）
- **移動は編集ではない**: 繰り返し一覧の「次回発生日へジャンプ」は mobile でも残した。ただし narrow ではドロワーがカレンダーに被るので、ジャンプ時に閉じる（閉じないと見えない日に飛ぶ）。`useRightSidebarOptional`（null 安全な方）を使う — セクション本体は Provider 無しで描かれても壊れてはいけない
- **監査は PR merge に間に合わなかった**（3 度目の教訓の派生形）: #484 は Stop hook のレビューゲートで role-qa を回している最中に merge された。**Blocking 2 件はどちらも自分が入れた退行**（date の commit-on-change / anchor 追従）なので、merge 後の追い PR で直した。**push 直後の `gh pr view` だけでは足りず、レビューを PR より前に回す**しかない
- **ローカルの共通ゲート 3 本（shared test / shared build / web build）は CI より狭い**（2026-07-30 実測 — PR #488 が CI で落ちて判明）。`.github/workflows/ci.yml` の実体は **7 ゲート** = docs-lint / **shared lint** / shared build / shared test / **web lint** / web build / **web test**。落ちたのは `shared` の eslint で、`react-hooks/refs` が **render 中の `ref.current = ...` を error にする**（捨てられる render が書き込みを残さないため）→ 更新を dep 無し `useEffect` に移して解決。**`web` の vitest はローカルに入っていなかった**（`npm install` 前で `vitest` 未解決 = exit 1・#475 で追加された後に install していない worktree では毎回踏む）。以後 PR 前に 7 本すべて回す
- **`scripts/docs-lint.sh` はローカルで偽陽性を出す**（2026-07-30 実測・role-qa の Blocking 指摘を実測で棄却）: Git Bash の grep 3.0 + `LANG=en_US.UTF-8` では `^Status:[[:space:]]*(enum)([[:space:]].*)?$` が**日本語を含む Status 行にマッチしない**（`LC_ALL=C` を付けると OK・CI の ubuntu では素で OK）。`2026-06-19-step1-desktop-daily-driver.md` は origin/main と同一内容で **CI は SUCCESS**。ローカル検証は必ず `LC_ALL=C bash scripts/docs-lint.sh`
- **`react-hooks/refs` のベースライン免除は 10 → 1 に減った**（#505 / PR #515 merge 済み。残るのは `useGraphInteraction.ts` の 1 本で、`simRef.current` を**依存配列の中で読む**別種の形。Issue #505 はそのため open のまま）。**パス完全一致の免除**なので分割・改名で失効して CI だけが落ちる性質は変わらない
- **Epic #290 の担当 Step は全て main 着地済み**（5-b #466 / 6 #468 / 7 #469 / 5-c #467 = PR #518 merged 2026-08-01）。closeout 依頼は **`section:schedule` の非 Epic open が 0 になってから** = 残り #520 の 1 件だけ。**Issue #517（MobileDrawer のフォーカストラップ）は `[refactor-core]` 宛てで当レーンの担当外**（PR #517 は存在しない — 旧記述の誤り）
- **merge 済み PR のブランチへ後から push しても main には届かない**（2026-08-01 実測・memory `push-after-merge-strands-commits` の再現）: PR #518 は 05:43Z に merge され、その 2 分後の push（`719e51d9`）は**ブランチにだけ乗った**。`gh pr view --json state,mergedAt,headRefOid` で **merge 時刻と merge された head を実測**し、自分の commit がそれより後なら `origin/main` から切り直して載せ替える。**並行してこうだいさん本人が同じブランチを触っている時間帯がある**ので、push が non-fast-forward で弾かれたら force ではなく merge で合流する（P-007）
- **待ちの読み方**: `#468 → #467` の順序は正しかった。両方とも `CalendarTab` の同じ領域（mobile 分岐 + sidebar portal）を書き換えるので、先に切っていたら真正コンフリクトだった
- **ブランチを跨ぐと per-chat memory も分岐する**（2026-07-30 実測）: #466 の記録は PR #480 側にしかなく、#469 ブランチへ main を取り込んだ時に memory / history が両方コンフリクトした（コードと i18n は自動マージ成功）。**解消は「両方のエントリを残す」**（片方を捨てると merge 済みの記録が消える）。**さらに PostToolUse formatter が conflict マーカーを markdown として整形する**（`=======` → `\=======` / `>>>>>>>` → `> > > > > > >`）ので、Edit 前に必ず現在の中身を Read すること（known-issue 026 と同種）

## 直近の完了

- **/goal 5 件（#1362 / #1370 / #1367 / #1373 / #1374）** ✅🟡（2026-09-01 — **#1411 / #1413 / #1414 merge 済み**、**#1424 / #1433 open**）。**一番効いた実測は「squash merge されたブランチの上に積むと、後で必ず偽コンフリクトを踏む」**: #1373 を #1367 の元コミットから生やしたところ、#1367 が squash で main に入ったせいで `c892f789` が main の祖先でなくなり、`git merge origin/main` が同じ 3 ファイルを衝突として突き返した。**解消が判断ではなく証明で済んだのは、merge base 以降にその 3 ファイルを触った main 側のコミットが #1367 の squash 1 本だけで、その中身が分岐元とバイト単位で同一だったから** — `git log --oneline <base>..origin/main -- <file>` と `git diff <元コミット> <squash コミット> -- <file>` の 2 本で「main は自分が既に持っているものしか持ち込まない」を示せる。示せない場合は全ハンク ours が危険になる。**#1373 の設計上の分かれ目は取り消し線**: MCP の `set_schedule_complete` は残す仕様なので、Event 側で取り消し線を素通しすると「消す手段が画面に無いのに永久に消える」ことになる。`variant === "task"` でゲートして 4 ビュー（AgendaList / WeekTimeGrid のブロックと終日チップ / MonthGrid）に同じ形で入れた。**#1374 は既定値を読み取り時に解決しない**（作成時に行へ書く）— 読み取り時フォールバックだと NULL が「通知しない」か「まだ決めていない」か区別できず、Settings の既定を変えた瞬間に過去の予定が勝手に再武装する。**通知はタイマーではなくスイープ**（`useMinuteClock` で 1 分に 1 回）: タイマーは Realtime bump のたびに張り直しで結局「もう出したか」の集合が要るうえ、Chromium がバックグラウンドで丸めスリープで消す。台帳は `useRef` ではなくモジュールスコープ（StrictMode の再マウントで捨てられ dev だけ 2 回出る）— 詳細は history 2026-09-01
- **#1343 詳細パネルのタブ 3 つを 1 行に揃える** 🟡（2026-08-31 — **PR #1355 open**）。**#1207 の続き**で、あちらは折れたラベルの高さを揃えただけ・折れること自体は残っていた。**CJK はどの 2 文字の間でも改行できる**ので詰まった flex 行は必ずどこかで折る — まず `whitespace-nowrap` で折るのを禁じ、溢れは `flex-wrap` でトラック側に吸わせる、が形になる（#1264 のツアーフッターが同じ手を先に取っていた）。**320px に実際に収めているのはパディングと文字サイズの方**で、`flex-1` が幅を決めるため**パディングは見た目にほぼ出ない**（長いラベルが取れる下限を下げるだけ）。**opt-in にしたのは他 4 呼び出し元のため** — `FrequencyEditor` の en "Every N days"（スマホ幅のシートに 4 セグメント）は折り返してよいラベル。**サイズ表を 2 本目として書いた**のは `cn` がただの文字列連結だからで、`px-*` を 2 つ載せると後勝ちではなく Tailwind の出力順で決まる（#830 と同じ罠）。`ScheduleSidebarTabs` はテストが 1 本も無かったので新規に追加
- **#1242 タグフィルタの aria-label に単数形を与える** 🟡（2026-08-30 — **PR #1267 open**）。`scheduleScreen.filterActive` が `{{count}}` を補間しながら 1 形しか持たず、タグ 1 件で "Filtered by 1 tags" と読み上げられていた。**画面上のどこにも出ない文字列**（`ScheduleToolbar.tsx:165` の `aria-label` 専用）なので目視では見つからず、実ブラウザ巡回の読み上げで初めて出た。**同じバグ・同じ直し方が #680（`materials.todos.todoCount`）で一度やってある**ので、テストもそのすぐ隣に同じ形で置いた。**単数形が無い `{{count}}` キーは他に 6 本残っていて、うち 5 本は aria-label ではなく画面に出る文字列**（`repeatFilterHidden` = 同じツールバーの可視ラベル / `usageCount` / `connect.itemCount` / `todoDeleteCascadeConfirm` = 破壊的確認ダイアログ / `childrenBlocked` = "subtask(s)" のごまかし / `work.sidebar.sessions` = 呼び出し側ゼロ）— **フォローアップ Issue の起票依頼を outbox へ**
- **#1173 カレンダー台帳 → タグフィルタ + グループ / #1207 ドロワーのセグメント揃え** 🟡（2026-08-29 — **PR #1226 / #1233 とも open**）。**一番の収穫は「新規テーブルを切らずに済む既存テーブルが眠っていた」こと**: `wiki_tag_groups` + `wiki_tag_group_assignments` は 0008 で作られたまま**コードからの参照ゼロ・本番 0 行**で、RLS も `supabase_realtime` publication も `REALTIME_TABLES` も済んでいた。DDL は 🛑 ユーザー push ゲートなので、新表を切ると **merge しても push まで機能が死ぬ**。流用でそれを回避しつつ、親 + join の正しいモデルも同時に手に入った。**「DDL が要りそう」と思ったら、まず既存の未使用テーブルを `list_tables` で実測する**
- **/goal 4 件（#1140 / #1124 / #1148 / #1153）** ✅🟡（2026-08-29 — **#1140 = PR #1163 merge 済み** / #1124 = PR #1168・#1148 = PR #1178・#1153 = PR #1187 が open）。**#1140** は `convertEventToRoutine` の meta bump が 0 行ヒットを見逃す件 — bump に `.select("id")` を足して**変換全体の role ゲート**にした（後続の attach は role を見ないので、bump が先に走ることが唯一の防具）。ついでに `permanentDeleteRoutine` の step 2 が取りこぼしを名指しして拒否するようにし、`useRoutinesAPI` は purge 拒否時に行を Trash へ戻す（今までは画面から消えて DB に残り、再試行の手がかりも無かった）。**#1124** は Schedule のツアー 5 ステップ + アンカー + ja/en コピー。全ステップが**実際の書き込み**で前進する。**#1148** は narrow の日リストを撤去して日付タップでドロワーがその日を開く形へ（#878 を覆す・作成ピルはサイドバー見出し行へ = `D-20260827-sched-1`）。**#1153** は Todo カンバンの退役（-4,600 行）でサイドバーのトレイが Todo のホームになり、詳細オーバーレイが本文と `[[` を引き取った — 詳細は history 2026-08-29
- **#1098 items_meta DELETE の role ガード + #889 CalendarTab 分割の完了** ✅🟡（2026-08-24 — **PR #1113 merge 済み** / **#1131 #1132 open**）。#1098 は DELETE 10 箇所（schedule 5 / routine 5）に `.eq("role", …)`。**実害があったのは caller から id を受け取って読み戻さない 3 つだけ**で、残り 7 つは「全部の DELETE が role を名乗る」という読み手が検算できる規則を保つための一様化（コメントで書き分けた）。`permanentDeleteRoutine` だけは外れ方が静かでない — 0011 の FK が NO ACTION なので step 2 が 1 件見逃すと step 3 が落ちる。**受ける側のトレード**（purge 失敗で残骸が残る < Todo を消して tasks_payload ごと連鎖で飛ばす）。#889 は **1,538 → 983 行**でビュー 4 本 + フック 5 本に分解し、2 本の return をレイアウト部品間の三項に統合。**挙動ゼロは className と `t()` キーの多重集合を機械照合して実証**。#1132 は #1113 のコメント 4 つの誤りと census の誤検知 2 種の訂正 — 詳細は history 2026-08-24
- **Mobile narrow の 2 件（#1036 追加パネルの溢れ / #1045 月ビューのドット打ち切り）** 🟡（2026-08-18 — **PR #1054 / #1059 とも open**）。#1036 は `TimeRangeField` の 2 列と 2 つの日付列に `min-w-0` を足しただけ（挙動・DOM・props 無変更）で、**原因はどこかの幅ではなく flex の自動最小サイズ**。#1045 は `MonthGrid` の compact 分岐に残り件数を出し、**文言は Desktop と同じ `formatMoreCount`**（en = `+N more` / ja = `他 N 件`・カタログ追加ゼロ）、**件数はそれぞれの上限で数える**。`CalendarTab` は無変更 — narrow は元から `formatMoreCount` を渡していて compact 側が呼んでいなかっただけ。両 PR とも CI の `verify` 全ステップ + `docs-lint` を exit 0 で実測 — 詳細は history 2026-08-18
- **main の赤を解除（担当外の mcp-server）** ✅（2026-08-17 — **PR #1031 merge 済み**）。`searchAll` の戻り値からドメインのキーが型ごと消えていた件。**テストを `as` でごまかさず handler を直した**（`Partial<Record<Domain, …>>`）— 既存の `as Record<string, unknown>` はまさに型の嘘を迂回するために書かれたキャストで、残すと直したものが見えなくなるので外した。新規 6 件は `as` を 1 つも使わず `.todos` を読む形で、**元に戻すと TS エラー 6 件でゲートが赤になる**ことを変異テストで確認
- **判断キュー 2 件の昇格 + #889 の 3 ユニット** ✅（2026-08-16 深夜〜17 — **PR #990 / #1016 / #1018 / #1022 すべて merge 済み**）。**一番の収穫は #1016 の実バグ**（Desktop に `<ConfirmDialog>` が無く確認が永久に返らない）で、**2 箇所で同じ集合を数え上げる形が生む壊れ方**そのもの。変異テストで実効も確認した（修正前の状態 = `!isWide &&` を戻すと Desktop の 2 ケースだけが落ちる）。#1018 は Todo 詳細面を **#736 のガードごと**移した（ガードが 400 行離れていたので、4 つ目の出口を足す人の目に入らない）。#1022 は繰り返し半分をフックへ = **jsdom に載らないせいで今日まで 1 行もテストできていなかった範囲**に 19 件（走査のタブガード / 生成してから移動する順序 / フィルタ解除が先）
- **監査由来 3 件 + #940 + #889 の 4 ユニット** ✅（2026-08-16 — **PR #964 / #966 / #968 / #972 / #974 / #976 / #982 / #987 すべて merge 済み**）。**#932 の要点 = 復元が拒否されるのは「壊れた」ではなく「その日にはもう占有者がいる」**なので、全体を落とさず衝突分だけ残して呼び出し側に返す形にした（`bulkRestoreScheduleItems` の戻り値を `{restoredIds, conflictedIds}` へ）。**#933 は現行 3 経路からは到達不能な欠陥**（`routines` に同一 id が 2 つ入らない限り成立しない）と実測したうえで service 側で塞いだ — 成立条件が「呼び出し元が id を重複させない」というどこにも書かれていない前提で、破れたときの代償（バッチ全滅・無言・リトライ運任せ）が全く見合わないため。**#934 は「兄弟同士を 1 件ずつ消す理由が無い」**（順序制約は events → routine の間だけ）。**#940 は `dateLabel`（読むだけの文字列）を date input に変え、4 つの submit を `ItemCreateSlot` 1 個に畳んだ** — これが無いとホストが日付を自分の state から読んで、ユーザーが選んだ日を無言で無視する。**#889 の 4 ユニットは全て「重複か、2 回読んでいるか」**（詳細フレームが 2 リテラル / 変換 150 行が最大ファイルに埋没 / 時計が壁時計を 2 回読む / レンズの「出ない条件」がコメントだけ）— 詳細は history 2026-08-16 (4)
- **#889 CalendarTab 分割 1/n（右サイドバー）** ✅（2026-08-16 — **PR #941 merge 済み**・main `8dd6691d`）。3 タブ（flow / todo / repeats）+ スイッチャー + レイアウト畳み込みを `web/src/schedule/ScheduleSidebar.tsx` へ。**2,578 → 2,446 行**。vitest 11 件は**マークアップではなく黙って壊れる規則**を固定（narrow の todo → flow 畳み込み / narrow で削除を渡さない / ルーチン集計 Desktop 限定 + その CTA が repeats への唯一の導線 / #466 フィルタ通知）。8 ゲート exit 0 — 詳細は history 2026-08-16 (3)
- **#893 Schedule 共有部品の props ドリルを畳む** ✅（2026-08-16 — **PR #936 merge 済み**・main `1d91096b`）。**WeekTimeGrid 28 → 6 / EventEditorPane 19 → 7 / ItemCreatePanel 12 → 6**。挙動変更ゼロの担保は「各部品が束ねた prop を**本体がこれまで使っていたフラットな名前に戻して**受け取る」形（デフォルト値も分解の右辺に据え置き）。**DoD の「既存テスト無改変」は文字通りには成立しないので、ケース本体を 1 行も変えずファクトリだけ畳む形に置き換えた**（詳細は上の進行中ブロック）。ステップ 2（Context 直接消費）は Briefing がセクション層 Provider の外にいるため不採用。ステップ 3 の実測で `dotColorClasses` のバイト単位重複 1 件を発見 → `scheduleVariantVisuals.ts` へ。8 ゲートすべて exit 0（shared 240 files 2232 pass / web 53 files 474 pass）— 詳細は history 2026-08-16 (2)
- **#897 SupabaseScheduleItemsService の一括系にテストを足す** ✅（2026-08-16 — **PR #929 merge 済み**・main `f82c451b`）。一括書き込み 4 本に vitest 19 ケース（5 本目 `updateFutureScheduleItemsByRoutine` は既存テストが押さえていた）。**一発で全部通ったので変異テストで実効性を実測した** — 重複排除の filter を外すと 3 件 fail / meta INSERT を消す + `deleted_at` を落とすと 6 件 fail。ついでに**クラスヘッダーが実装と食い違っていた**のを訂正（「ON CONFLICT ignoreDuplicates を使う」と書いてあるが実装は事前 SELECT 方式。PostgREST は **PARTIAL** unique index に ON CONFLICT を向けられない）。複合キーの綴りが 2 箇所に散っていたのを `routinePairKey()` に一本化。**Issue のステップ 2（bulkCreate と updateFuture の共通部分抽出）は共通の中核が実在しないので見送り** → 判断キュー D-20260816-sched-1。**独立監査 2 本（role-qa + sync-auditor）で Blocking 0 / 指摘 4 件を追いコミット `0eef4dd3` で全反映** — doc のドリフトが 1 箇所ではなく 4 箇所（うち `db-conventions.md` §10.8 は**そのレシピを規約として処方している**側）／ テストが見ていない契約 4 件（事前チェックのページング・dismissed 日の再生阻止・手動イベント判定の空振り・未認証経路）。監査が見つけた**既存欠陥 3 件は outbox へ起票依頼**（最優先 = Trash からの復元が partial UNIQUE で無言失敗）— 詳細は history 2026-08-16
- **#878 Mobile Calendar のメインを月ビューに** 🟡（2026-08-15 — **PR #916 open**）。メイン = 月グリッド + **その下に選択日のリスト**（日別リストの置き場所はユーザー選択 = 2026-08-15）。#692 の月シートと日付横シェブロンは i18n キー 2 本ごと退役。**narrow の `effView` は常に `"month"`** なので矢印が月送りになり、日はセルタップで選ぶ。`MonthGrid` に任意の `selectedKey` を追加（**印はセル側** = today バッジと両立させるため。未指定なら `aria-selected` すら出さない — 全セルが false を名乗ると「選ぶものがある」と読み上げられる）。`docs/requirements/mobile-scope.md` の #4 行も同 PR で更新 — 詳細は history 2026-08-15
- **#877 Mobile の Schedule todo に設定日付が出ない** 🟡（2026-08-15 — **PR #915 open**）。narrow では todo 詳細シートが todo に触れる唯一の入口なのに、日付だけ一言も出ていなかった。`TodoDetailPanel` に**読み取り専用**の日時行（キャプションと値は対で必須）。**値はチップと同じ導出**（`todosToCalendarChips` から `todoScheduleSlot()` を切り出し）— 終了時刻が無ければ 60 分・退化スパンは終日、という外から見えない 2 規則があり、実装が 2 つあるとそこだけ食い違う — 詳細は history 2026-08-15
- **#870 時刻変更 + 繰り返し ON を 1 Save でやるとテンプレート時刻が変更前になる** 🟡（2026-08-15 — **PR #900 open**）。エディタは「繰り返し → フィールド patch」の順で送る（フィールド側が scope ダイアログを出すので最後でなければならない）ため、変換時の `selected` はまだ変更前の時刻を持つ。**Save がフィールド patch を繰り返しと一緒に渡し、変換分岐がそれを種に重ねる**形にした。種は 1 つ（テンプレート / 楽観 routine / 変換が確保する日付が全部そこから派生）なので片方だけ新しい値になれない。**実際に動いたキーだけ**重ねる（未編集キーを `undefined` で潰さない）— 詳細は history 2026-08-15
- **#708 繰り返し削除の Undo でカスケードごと戻す** ✅（2026-08-13 — **PR #858 merge 済み**）
- **#789 Kanban 詳細から削除しても rightSidebar の殻が残る** ✅（2026-08-13 — **PR #798 merge 済み**）。削除と「予定に変換」はどちらも**行そのものが盤面から消える出口**なので、後始末を `closeDetailShell`（選択解除 + wide のみ `rightSidebar.close()`）に集約した。**ヘルパーに寄せたのは 2 経路が割れることがこのバグの形だから** — 削除だけ直すと今度は変換と食い違う。narrow は無変更（detail = BottomSheet で、殻は `isWide` の effect の担当）。テストの tree モックに `refetch` が無く、**変換の成功分岐が例外で失敗バナーへ流れていた**ので併せて追加 — 詳細は history 2026-08-13
- **#774 別の日を見ていても「今日の予定はありません」** ✅（2026-08-13 — **PR #804 merge 済み**）。判定を `web/src/schedule/agendaEmptyLabel.ts` に切り出し（CalendarTab は Provider 一式が要って描画テスト不可 = `taskChipPanel` / `unsavedCloseGuard` と同じ理由）。**Dayflow タブは `emptyToday` のまま**（あちらは本当に今日のリスト）。`TranslationKey` は en catalog の型から導出されるので **union の手書き追加は不要**（Issue 本文の指示は現状と食い違う）— 詳細は history 2026-08-13
- **#790 Todo 削除ガードと翻訳キーが schedule/ に置かれたまま** ✅（2026-08-13 — **PR #813 merge 済み**）。`todoTrayDeleteGuard.ts` を `web/src/shared/` へ、3 キーを `taskDetail.*` へ移動。**確認ボタンの `scheduleScreen.delete` も `taskDetail.delete` を新設して差し替えた**（Issue が名指ししたのは 3 キーだが、DoD「Tasks 側の削除文言がセクション非依存のキーになる」に確認ボタンも含まれると読んだ。過剰なら戻すのは 3 行）。**#798 と同じ `kanbanView.test.tsx` を触るので merge 順序に注意** — 詳細は history 2026-08-13
- **#628 詳細編集パネルの保存ボタン** ✅（2026-08-11 — **PR #681 merge 済み**）。D-20260810-sched-1 = A（ボタンのみで確定・blur は draft・未保存クローズ確認）。draft は「触ったフィールドだけ」を item に重ねる方式（リモート更新が未保存扱いに化けない）。系列/当日混在の保存は scope ダイアログへ**丸ごと**預ける（キャンセルで全破棄・undo 1 件）— 詳細は history 2026-08-11
- **#625 Event ↔ Todo 相互変換** ✅（2026-08-11 — **PR #684 提出・CI 走行中**）。id 維持・変換順序は「新 payload UPSERT → role UPDATE → 旧 payload DELETE」（QA と sync-auditor が独立に同結論。旧 DELETE 先行案の根拠だった FK 制約は実在しなかった）— 詳細は history 2026-08-11
- **/goal 一括消化 6 件（#633 / #592 / #593 / #626 / #573 / #572）** ✅（2026-08-10 — **PR #637 / #639 / #645 / #648 / #652 / #654 全 merge・Issue close 済み**）。#648 の後追いレビュー修正 = PR #659 も merge 済み — 詳細は history 2026-08-10 / 2026-08-10 (2)
- **#569 タスクチップ操作の Undo（place / move・resize / 今日に追加）** ✅（2026-08-02 — **PR #581 merge 済み**）。`updateNode(id, updates, { undoLabel })` の **opt-in 方式**（全 updateNode を一律 push にしない — Tasks ボードのタイトル入力が打鍵ごとにスタックを埋めるため）。**値が変わらない書き込みは push しない**（元の枠に戻すドラッグが直前の undo を食い潰す）。チップ操作 → updateNode 引数の決定は純関数 `web/src/schedule/taskChipUndoWiring.ts` に切り出してテストで pin（QA 実測: 切り出し前はラベル削除も place/move 反転も全ゲート素通りだった）— 詳細は history 2026-08-02 (2)
- **#565 Todo タグ別ビューの 3 列化** ✅（2026-08-02 — **PR #579 merge 済み**）。`repeat(3, minmax(220px,1fr))` + 下限割れは横スクロール（220px = タグ列ヘッダーの ColorPicker swatch 幅。QA が到達条件 1280px + ナビ + 右サイドバーを算術で実測）— 詳細は history 2026-08-02 (2)
- **#563 週ビューの列線ずれ** ✅（2026-08-02 — **PR #577 merge 済み**）。原因はスクロールバー幅で時間グリッドだけ列割りの分母が狭くなること。3 バンドを同一スクロール箱 + sticky にして同じ幅を割る構造解。**#562 の DnD 境界 2 点の追従が今回差分の本丸**で、QA の変異実測（旧参照に戻しても 15/15 素通り）を受けて rect スタブの座標テストで pin — 詳細は history 2026-08-02 (2)
- **#568 Undo/Redo が今日以外の予定に効かない（sev:important）** ✅（2026-08-02 — **PR #576 merge 済み**）。today 固定 provider に `ScheduleItemsViewMirror`（find / upsert / patch / remove）の窓を 1 個登録し、prev 解決を items → mirror の 2 段に。undo/redo の書き戻しも mirror 経由で rangeItems へ即時反映。**書き込み順は「provider 先・ローカル patch 後」を契約化**（同期 mirror でも prev が壊れない唯一の順序。本番 mirror は effect 更新の ref なので偶然どちらでも動く — 因果ではなく不変式としてコメント）— 詳細は history 2026-08-02 (2)
- **#562 終日チップの drop 復元 + グリッド移動のクランプ** ✅（2026-08-02 — **PR #570 merge 済み**）。終日→時間グリッドへのドラッグが y 座標を時刻に丸めて 01:30 / 00:00 を捏造していたのを `onDropAllDay` で終日レーンの drop として commit する形に。move は可視時間窓でクランプ。既存の壊れた行（end<=start の退化 span）は終日候補チップとして救済表示し drag / トレイ / Tasks 側から再配置可能に — 詳細は history 2026-08-02
- **#553 TimeRangeField（時間帯編集の共有部品）** ✅（2026-08-02 — **PR #567 merge 済み**）。start<end 不変式を部品が所有・EventEditorPane は `onChangeTimes` で 1 操作 = 1 書き込み（routine のスコープダイアログが 2 回出ない）— 詳細は history 2026-08-02
- **#551 左/右クリックのアイテム操作パネル統一** ✅（2026-08-02 — **PR #566 merge 済み**・net −244 行）。ItemActionPopover に一本化・`ScheduleItemContextMenu` / 汎用 `ItemContextMenu` を撤去・詳細編集 tagSlot に TagColorControls — 詳細は history 2026-08-02
- **#555 Todo トレイの削除 + タグ付け外し** ✅（2026-08-02 — **PR #561 merge 済み**）。TodayTodoTray に optional 削除（softDelete → Trash 復元可）+ renderRowExtra スロット。Briefing ホストは props 未指定で描画不変 — 詳細は history 2026-08-02
- **#520 パレット移動時にレンズ + 繰り返しフィルタを外す / #524 グラフのコールバック凍結** ✅（2026-08-02 merge 確認 — **PR #533 / #536 とも merge・Issue close 済み**。実装の詳細は history 2026-08-01 (2)）
- **#508 BottomSheet にフォーカストラップと初期フォーカス（shared-fix `[all]`）** ✅（2026-08-01 — **PR #516**・branch `claude/schedule-508-sheet-focus-trap`）。**`[all]` は着手宣言コメントを先に付けてから着手**（#473 で 40 分の二重実装をやった型）。Modal が既に同じ機構を持っていたので **`shared/src/hooks/useDialogA11y.ts` に切り出して両方から使う**（複製しない）。**最大の発見はレイヤ順序が listener 登録順で決まっていたこと** = 全ダイアログが document で待つので**最初に登録した＝一番古い外側**が先に走って stopPropagation する → シートの上に開いた Modal の Esc がシートに渡っていた。**module レベルの layers スタックで「最前面だけが反応する」に変更**。BottomSheet は body scroll を**ロックしない**（背後のリストをスクロールしたまま使う想定・Modal だけ `lockScroll: true`）。**初期フォーカスは子が既に取っていたら触らない**（QuickAddSheet が effect で input を focus する = 二重フォーカスの正体）。**可視判定の `offsetParent !== null` は jsdom で全滅する**ので layout がある時だけ適用（無いと trap がテストから見えない = #475 の形）。7 ゲート全緑（shared 167 files / 1371 pass・web 8 files / 75 pass）
- **#505 `react-hooks/refs` のベースライン免除を 10 → 1 に** ✅（2026-07-31 — **PR #515 merge 済み**。Issue は残り 1 ファイルのため open 継続）。違反は 3 形しかなく、7 箇所は dep 無し `useEffect` へ、1 箇所は `useState(() => …)` の lazy init へ、`useFrozenNoteSortKey` だけは**render 中スナップショットが意図的**（effect にすると保持されない 1 レンダーが通り #366 が再発する）なので state を render 中に調整する形へ移した — 詳細は history 2026-07-31 に全文
- **#504 routine template の更新失敗が無言** ✅（2026-07-31 — **PR #514 merge 済み**）。**rollback ではなく書き込み順序で解いた** = template を先に書いて落ちたら中断すれば、失敗時点で occurrence に触れていないので「何も保存されていません」が嘘にならない。順序と中断規則は `shared/src/utils/seriesEditSequence.ts` に切り出して vitest で pin。文言分岐はネスト三項ではなくテーブル（新しい reason が黙って最後の `else` に落ちない）— 詳細は history 2026-07-31 に全文
- **#503 コマンドパレットのアイテム横断検索** ✅（2026-07-31 — **PR #513 merge 済み**）。土台は `[[` 補完の候補プール（`useLazyStalePool` に切り出して両方から使う）。あいまい検索は入れず前方一致 → 部分一致の 2 段・空クエリは 0 件・上限はロール単位。**予定だけ「日付」を intent に載せる**（カレンダーは可視範囲しか取らず、ナビゲーションが範囲外を生成しないため）— 詳細は history 2026-07-31 に全文
- **#468 Step 6 カレンダー台帳をグリッドのタグフィルタとして配線** ✅（2026-07-31 — **PR #506 merge 済み**・main `37691157`）。**カレンダーは「入れ物」ではなく life-tag 1 本への保存済みビュー**なので、所属判定は「そのタグを持っているか」に還元される。**繰り返し由来のオカレンスは系列（`routineId`）のタグを継承する** — オカレンス行は再生成されるので行に付けたタグは materialise のたびに消える。**実装前にアプリ内へ予定へタグを付ける導線が 1 本も無かった**（`assignTagToItem` の呼び出し元は Notes 詳細とタスク Kanban だけ）ので、`EventEditorPane` に `tagSlot` prop を足して既存 `TagPicker` を注入した（これが無いとレンズを入れた瞬間に予定が全部消える）。**タスクチップにもレンズを効かせる**（タスクは Kanban で同じ life-tag を持てるので「タグを持てないから対象外」は成立しない）。**永続化しない**（#466 と同じ理由 — 骨組みが欠けたカレンダーで空きスロットに二重予約する）。2 フィルタは独立 AND で**各自が自分の hidden 件数だけを数える**（チェーンすると二重計上して「N 件を非表示」が実際の欠落を超える）。**タグを soft-delete された台帳はレンズに出さない**が、「未読込」「取得失敗」も lookup ミスとしては同型なので `tagsLoading` で判定をゲートした
- **#466 Step 5-b Calendar グリッドの繰り返しフィルタ** ✅（2026-07-30 — **PR #480 merge 済み**・main `9ff4a813`。実ブラウザ検証は chat-main に残）。粒度は **案 A「繰り返し由来を隠すトグル」を採用**（却下 = 案 B「この繰り返しだけ表示」— #408 の一覧が頻度 + 次回日付で同じ問いにほぼ答えているため見返りが薄い）・**永続化しない**（起動時に骨組みの無いカレンダーが出ると、空いて見えるスロットに二重予約する事故になる）。**フィルタは view 層 1 箇所（`gridRangeItems`）だけ**で `rangeItems` は素のまま = 隠しても編集が書く内容は変わらず、隠れた行は flow タブから編集できる。**`applyRepeatFilter` が `{ visible, hiddenCount }` を返す**のでツールバーの「N 件を非表示」がグリッドと食い違えない。ON 中は選択が routine 由来なら選択と popover を落とす
- **#411 Todo ボードを Materials から Schedule へ移設** ✅（2026-07-28 — **PR #454 merge 済み**・main `218d8dab`）。Materials = Notes/Daily、Schedule = Calendar/Todo。**実質の本体は導線 4 本の全数追随**（nav ショートカット / `[[link]]` 着地 / `global:new-task` / コマンドパレット）で、1 本落とすと「タスクへ飛ぶ」が黙って死ぬ。Kanban が要る TaskTree + WikiTags は Schedule 側に既に居た（カレンダーの task チップ用）ので 2 組目を入れ子にせず再利用した。narrow はハンバーガーを足さず SegmentedControl だけ（Calendar は自前で描き、Todo は 768px 未満で drawer を閉じるため）。4 ゲート exit 0（153 files / 1257 pass）。実ブラウザ検証は chat-main に残
- **#408 Routines ヘッダータブ退役 + rightSidebar 繰り返し一覧** ✅（2026-07-28 — **PR #452 + QA 回収の #453 ともに merge 済み**・main `43905c6d`。実ブラウザ検証は chat-main に残）。**失われる操作 5 件のうち (1) 空 routine の新規作成 / (3) テンプレ直接編集は意図的に退役**（(1) は CLAUDE.md §4「UI 上は Event + 繰り返し設定」方針、(3) は #279 scope ダイアログが代替）、**埋めたのは (2) 到達性 / (4) テンプレ削除 / (5) order 俯瞰**。**(2)+(4) を落とすと実行日ゼロの routine が永久に到達不能**（オカレンスが無いので Calendar から選べず、Routines タブも無い）— #407 のゾンビ 2 件が実在するので机上の穴ではない。`nextRoutineOccurrence` の **null が「この行は飛び先が無い」の判定そのもの**で、その行はボタンにせず span にする（#434 S-1 と同じ「押しても何も起きないコントロールは壊れて見える」原則）が削除ボタンは残す。`useRoutinesAPI.createRoutine` は UI 呼び出しゼロでも残した（DataService 層の API であって死んだ UI コードではない）。QA ラウンドの反映は PR #453（進行中）
- **#434 pending 表示 + 失敗 toast**（**PR #450 merge 済み**・main `e43275d9`。merge 後に `git grep origin/main` で `repeatMaterialiseFailed` と `aria-disabled` の着地を実測）— 詳細は下の行と history 2026-07-28 に全文
- **#434 #407 QA follow-up 2 件（変換中の pending 表示 + 失敗 toast / ガードの shared 切り出し）** ✅（2026-07-28 — **PR #450** 作成済み・merge は 🛑 ユーザーゲート）。**見送り可だった 2 も実装した**: 1 で state ミラーが増え、ref と state がずれると「ずっと変換中」か「一度もロックされない」に化けるので、テストで固定する価値が出たため（#355 `useDeferredAction` と同じ「web にテストランナーが無いから shared の純フックへ寄せる」方針）。新設 `shared/src/hooks/useInFlightGuard.ts` の肝は **`begin(id)` が check と claim を 1 呼び出しに畳む**点で、「has で見てから add」の 2 文に戻せない＝ #407 を生んだ check-then-act の隙間を呼び手が再導入できない。**同期の ref が権威・state は描画専用のミラー**（2 クリックが同じ tick に届く以上、バッチされる state では 2 個目を素通しする）。UI 側は `FrequencyEditor` の `pending` で全コントロールを disabled + `aria-busy` + `role="status"` の理由表示、`SegmentedControl` に `disabled` を追加。失敗 toast は `onRepeatConvertFailed` 注入 →`CalendarTab` が `showToast` に配線（#376 `noteAttachFailed` と同契約）。**role-qa アドバーサリアルラウンド反映済み**（2 コミット目 `69ca32e6`・B=0 / S=2 / N=4 で S 2 件とも修正）: **S-1 は自分が入れた退行** = 本物の `disabled` で節をロックすると focusable がゼロになり、キーボード操作中にフォーカスが `<body>` へ飛んで戻らない → セグメント / 曜日チップは `aria-disabled` + ハンドラ無効化（tablist の roving tabindex を保つ）、入力は `readOnly` に変更（**先例 `Menu.tsx:185` は `disabled` と `aria-disabled` の併用だが、tablist は全segment が同時にロックされるので併用だと退避が起きる — 先例をそのまま真似ない判断**）。S-2 = `ensureRoutineItemsForDateRange` の reject が void promise の外に抜けて unhandled rejection ＋ `reload()` スキップ → catch して `reload()` を `finally` へ移動（どの出口でもちょうど 1 回）。**この経路は繰り返し自体は成立している**ので toast 文言を理由で出し分ける `onRepeatConvertFailed(reason: "attach" | "materialise")` に変更。検証 = shared vitest **151 files / 1234 pass（main 比 +25）**・shared build・web build・web lint すべて exit 0。**スコープ外にした観察**（PR 本文に記載・未起票）= 系列パス（既に routine が付いたアイテムの頻度変更）の `updateRoutine` false は今も静かに降りる（`useScheduleMutations.ts:516`）
- **#433 #407 のアドバーサリアル QA 修正が main に届いていない（置き去りコミット回収）** ✅（2026-07-28 — **PR #435 merge 済み・Issue close 済み**。merge commit `8075adea`・`git merge-base --is-ancestor` で main 到達を実測、`routineFrequency.ts:115` の `if (!start)` と `frequencyEditor.test.tsx:107` の空文字テストが origin/main 側に存在することも実測。旧ブランチとの対象 7 ファイル差分はゼロ）。**残 1 件** = `claude/schedule-407-repeat-desync` の削除（tip `52b6d081`・着地確認済みなので安全。Claude 側は権限で実行不可のためユーザー作業）。PR #423 の merge（7/27 18:20 JST）**直後**に push した 2 コミット（`a873e583` 18:35 / `52b6d081` 18:37）が main に届いていなかったのを、`origin/main`（`415cb185`）から切った `claude/schedule-433-recover` へ cherry-pick で回収。**両方ともコンフリクトなしで乗った**。着地前の main は「#423 の fail-closed 判定 + `FrequencyEditor` の空文字 emit」が噛み合い、**開始日をクリアすると reconcile が未来行を掃除する**退行が起きうる状態だった。検証 = shared vitest 149 files / 1209 pass・shared build・web build・**web lint も exit 0**（下の引き継ぎメモにある `NotesView.tsx:291` の main 由来 lint error は 2026-07-28 時点の origin/main `415cb185` では**再現しない**。どの PR で消えたかは未追跡）
- （#407 繰り返し表示の不整合は PR #423 + #435 で main 着地済み・history の 2026-07-27 / 2026-07-28 に全文あり。**DB クリーンアップだけ残る** = Routines タブから「新規予定」routine 2 本 `3c4a1f09` / `b15eb258` をユーザーが削除する。コード修正で新規生成は止まっているが、routine 本体と生成済み si- 行は UI からの削除が要る）
- （#367 見送り決着（PR #403 merge 済み）/ #376 統合アイテム生成パネル（PR #393 / #395 / #400 merge 済み）は history の 2026-07-27・2026-07-26 に全文あり。3 件枠に収めるため間引いた）
- #298 Step 3 rightSidebar 本日の Todo tray ✅（2026-07-23 — PR #323 merge 済み・main `5f9abf48`）。**history 側にエントリが無いのはここだけ**なので、この行を消すと merge 済みの記録がこの worktree から消える（#296 / #297 は history の 2026-07-20 に残る）
  （#352 / #353 / #354 / #355 / #385 / #299 は history の 2026-07-25〜26 に全文あり。ここからは間引いた）

## 予定

- **#1433（#1374）は merge より先に 🛑 `supabase db push` が要る**。`supabase/migrations/0027_events_payload_reminder_offset.sql` はローカルファイルとして置いただけ（`apply_migration` MCP 不使用）。**新列が `EVENTS_PAYLOAD_COLUMNS` の SELECT 一覧に入るため、列が無い本番にコードだけ乗ると schedule の SELECT が全部 PostgREST 42703 で落ちる**。オフラインの CI ゲートはこれを捕まえられない
- **#1373 が「進捗の数字」を 2 つ凍らせた**（フォローアップ Issue の起票依頼を outbox へ）: 予定の完了概念を外したので、`{done}/{total}` カウンタと `RoutineSummaryCard` のバーが Event 由来分について動かなくなる。**データと MCP の書き込み経路（`set_schedule_complete` / `completed` 列）は温存してあるので、数字の出し方を Todo 側だけに寄せるか、別の指標に替えるかは仕様判断**
- **squash merge されたブランチの上に積んだら、後で偽コンフリクトを踏む**（2026-09-01 実測 = #1373 が #1367 の上）。**避けられないので、解消時に「main が持ち込むものは自分が既に持っているものだけ」を 2 本のコマンドで証明してから ours を採る**: `git log --oneline <merge-base>..origin/main -- <file>`（main 側で触ったコミットの列挙）と `git diff <分岐元> <squash コミット> -- <file>`（中身の同一性）。**証明できないファイルは ours 一括にせず手で読む**
- **`{}` を返すダミー DataService は新しい呼び出しを足した瞬間に落ちる**（2026-09-01 = #1374 の `scheduleItemsCRUD.test.tsx`）。**そのとき本番側も一緒に疑う** — このケースは追い書きの失敗が `onSaved(null)`（= 作成失敗）として伝わっていて、**行は保存済みなのにエディタが開きっぱなしになる**実バグだった。追加の書き込みを既存の write の後ろに繋ぐときは、失敗の伝わり先を必ず見る
- **`public.calendars` の DROP は別 PR（🛑 ユーザー `supabase db push` ゲート）**。migration 0025 + publication からの除去 + `syncRealtimeTables.test.ts` の drop 構文パーサ追加（0017 の array に残るので union 比較では表現できない）+ ハードカウント再計算 + `REALTIME_TABLES` / `TABLE_DOMAIN` からの除去 が**同時に**要る。chat-main への起票依頼は outbox に記載済み
- **section:schedule の open は #1124 / #1148 / #1153 の 3 PR 待ち**（2026-08-29 時点）。**#1148 と #1153 は `ScheduleSidebar.tsx` / `CalendarTab.tsx` / `scheduleCopy.ts` で衝突する**（前者は flow タブ、後者は todo タブを触る）。**#1124 と #1148 も `CalendarNarrowLayout.tsx` で衝突する**（前者が日リストの `AddPill` に `tourId` を足し、後者がそのブロックごと消す）。解消はどれも後着地側で、**#1148 側の解決は「ブロックを消し、`tourId` をサイドバーの `AddPill` へ持ち越す」**
- **#1124 のツアーアンカー 3 本は #1153 で面ごと消える**（`scheduleTodoTab` / `scheduleTodoAdd` / `scheduleTodoBoard`）。両方 main に乗った時点で、サイドバーの todo タブ / 作成ピル / トレイ本体へ**付け替える追随 PR が要る**（計画書 `2026-08-29-schedule-todo-tab-retirement.md` の Risks 節に記載済み）
- **section:schedule キューは #889 だけが残る**（2026-08-16 夜 実測）。#932 / #933 / #934 / #940 は merge 済みで close 見込み。**タイトルの `[refactor-core]` 接頭辞で担当を判断しない** — ラベル `section:schedule` が正
- **`gh run rerun` は base が直っても意味がない**（2026-08-17 実測）: pull_request の run は**記録済みの merge commit をそのまま再生する**ので、main が直っても古い base を掴んだまま同じエラーで落ちる（`gh run view <id> --json headSha` が変わらないのが目印）。base の修正を反映するには**新しい pull_request イベント**が要る = ブランチへの push、または close → reopen
- **close → reopen で直らないときは merge ref を実測する**（2026-08-17 実測・#1030 が 2 回失敗）: `git fetch origin refs/pull/<N>/merge` で GitHub が実際に建てている merge commit を取り、`git show <ref>:<path>` で修正が入っているか見る。**#1030 は GitHub 側の merge ref 再計算が遅れていただけ**で、コードの問題ではなかった（#1029 は 1 回で通った = 再計算が先に済んでいたから）。**再発火の前にまず「GitHub は今どの base で建てているか」を確かめる**
- **判断キューの open はゼロになった**（2026-08-16 深夜・PR #990）。D-20260816-sched-2 / sched-3 はどちらも A で台帳へ。**sched-2 の波及**: 部品の prop / 引数の形を変える PR では、**テストのケース本体を動かさずファイル冒頭のファクトリだけで吸収する**（#889 以降も同じ形）
- **#1016 と #1018 は `CalendarTab.tsx` で衝突する**（`git merge-tree` で実測・CONFLICT 1 件）。**#1022 はどちらとも衝突しない**（触る範囲が別）。**#1016 を先に**（バグ修正のため）。どちらも main から独立に切ってあるので先発は素通りし、後発だけ main を取り込む
- **ユニット単位の抽出では 1,000 行に届かない**（2026-08-16 実測 — 上の進行中ブロック参照）。1 ユニット 110〜180 行なので、`Closes #889` までにあと 6 本以上。**行数を目的にして大きく切ると、挙動変更ゼロの担保が薄くなる**ので単位は崩さない
- **同じホストを触る PR を 5 本並行させたら、実際に衝突したのは 1 本だけだった**（#982 が #976 と）。**衝突したのは「同じ行を別の理由で触った」ペア** = #976 が変換 150 行をフックへ移し、#982 は同じブロックの依存配列に名前を足していた。解消は main 側を丸取り（依存配列の追記はコードごと移動済み）。**別々の塊を触る限り 4 本並行は成立する**が、片方が塊ごと動かす PR なら順序を決めるか、後発が main を取り込んでから作る
- **`git add -u` は merge 解決中に他レーンの tracker を巻き込む**（2026-08-16 実測）: merge が持ち込んだ `chat-settings-refine` 等が staged になり pre-commit-tracker-guard が正しくブロックした。**merge commit では unstage すると merge が壊れる**ので、この場合だけ `[tracker-ok]` を付けて理由をメッセージに書く（自分の tracker を実装に混ぜる免罪符ではない）
- **判断キュー D-20260816-sched-3 が open**（#933 の R2 全滅を撃ち直しに変えるか）。放置時は現状維持で、#966 の merge はブロックしない
- **`CalendarTab.tsx` は #878（PR #916・merge 待ち）と #889 が同じホストを触る**。#889 に着手する前に `git fetch` して #916 の着地を確認し、着地後の main から切る（先に切ると真正コンフリクトになる）。**#893（PR #936）も CalendarTab を触る**ので、#936 の着地も同じく確認する
- **`MonthGrid` 16 props / `TodayTodoTray` 12 props** は #893 の対象外だが同じ手当てが効く（2026-08-16 実測）。必要なら別 Issue で
- **outbox 2026-08-10 に起票依頼 2 件**: ① Notes / Tasks 詳細シートの `vh` → `svh` 展開（#633 の水平展開）② mobile の Todo チップ詳細シート（#564 / #626 follow-up）
- **chat-main への起票依頼 2 件が outbox 2026-08-02 (3) に積んである**（どちらも QA の実測付き既存欠陥）: ① series 編集（this-and-future / all）の undo がアンカー 1 日だけ戻る ② TaskTree undo の全ツリースナップショットが後続の silent 書き込みを巻き戻す（タイトル入力中 Ctrl+Z で踏める）
- **#411 で「タブを 1 つ減らしてから 1 つ足す」順序は正解だった**: #408 が `ScheduleTab` 型・`scheduleTab` state・タブ帯を撤去した直後に #411 が同じ構造を `"calendar" | "todo"` で足し直したので、中間状態が常に 1 セクション 1 構造で済んだ。**ただし `ScheduleTab` の置き場所は #408 前と変えた** = `ScheduleScreen.tsx` が export し `MainScreen` が import する（switch する側と型を同居させる）
- **【この worktree で最重要の実測】カレンダーのナビゲーションは occurrence を生成しない**（2026-07-28 #408 の QA で判明・B-1）。範囲移動は `useVisibleRangeItems` の `loadDateRange` で**取得するだけ**。生成経路は 3 本のみ = `RoutineScheduleSync`（`ScheduleItemsContext` の `date` = 既定今日）/ 変換時 / reconcile（後 2 者はどちらも**その時の可視範囲**だけ）。**`useScheduleItemsRoutineSync.ts:260` と旧 requirements の「窓の外は表示時に `ensureRoutineItemsForDateRange` が拾う」は嘘**（その配線は存在しない）。よって「未来日へ飛ぶ」導線を作るときは必ず自前で ensure を打つ — #408 の一覧はこれを踏んで `handleOpenRepeat` で明示的に打っている
- **`deleteRoutine` は失敗しても throw しない**（楽観削除 → `logServiceError` で握り潰し → 正常 resolve）。#408 で戻り値に `landed: boolean` を追加（`updateRoutine` の `landed` と同じ idiom・既存の `deletedScheduleItemIds` は温存）。**`softDeleteRoutine` の undo は非対称** = `restoreRoutine` は routine の `items_meta` 1 行しか戻さず cascade された occurrence は trash に残るので、一覧の削除は 2 段確認にした
- **`nextRoutineOccurrence` は `routineScheduleSync.ts` にある**（`routineFrequency.ts` ではない）。`shouldCreateRoutineItem` を呼ぶ必要があり逆向き import は循環になるため。「そこに occurrence ができるか」は**生成器と同じ答え**でなければならない（頻度だけ見ると archived / hidden な routine にも飛び先が出て、着地は必ず空になる）
- **Todo の導線は 4 本ある**（#411 で全数追随済み。今後タスク画面を動かすときはこの 4 本を必ず洗う）: nav ショートカット（`handleNavigate`）/ `[[link]]` の着地（旧 `MATERIALS_TAB_FOR_ROLE` → `ITEM_NAV_TARGET`）/ `global:new-task`（`handleNewTask`）/ コマンドパレット。**`[[link]]` の着地マップは section+tab の組を返す形に変えた** — 3 role が 1 セクションを共有しなくなり、タブ名だけでは行き先が決まらなくなったため
- **`nav:schedule` と `nav:tasks` は両方ともタブを明示 set する**（#411）。片方だけだと `nav:tasks` の直後に `nav:schedule` を押してもセクションが既に schedule なので何も起きず、Todo に貼り付く。**素の「Schedule」セクション遷移（サイドバー / パレットのセクションエントリ）は逆に sticky のまま** = Materials の慣習に合わせて最後のタブを保持する
- **実ブラウザ検証を chat-main へ依頼する**（#376 の 3 タブ・既存タスクの配置・**既存ノートを選んだときにリンクが Connect / Notes 側に出るか** — ここが #400 で直した経路。**#407 も追加** = 繰り返し「なし」でアイコンが消え、日をまたいでも「新規予定」が復活しないこと）。§7.4 の localhost 集約ポリシーによりこの worktree では実測できない
- **#407 の merge 後クリーンアップをユーザーへ案内済み**（Issue コメント + PR #423 本文）: Routines タブから「新規予定」routine 2 本（`3c4a1f09` / `b15eb258`）を削除。コード修正だけで新規生成は止まるが、routine 本体と生成済み si- 行は UI からの削除が必要
- Epic #290 の残 Step（Step 5 構成再編 / Step 6 カレンダー台帳配線 / Step 7 エディタ拡充）は未起票。Step 5 の子 Issue が無ければ chat-main へ起票依頼を outbox へ
- chat-main へ起票依頼済み（outbox 2026-07-26 の 3 通）: (1) ~~`web/src/notes/NotesView.tsx:291` の lint error（main 由来）~~ = **2026-07-28 時点で解消済み・起票不要** (2) Mobile 月表示で FAB が `mobileSelectedDay` ではなく `anchorDate` に作る (3) 生成直後の楽観行が同期リフェッチで消えると開いたばかりの詳細エディタが閉じる

## 引き継ぎメモ（この worktree で効く事実）

- **`wiki_tag_connections` へのリンクは「FK 先の行が DB にできてから」しか書けない**（2026-07-26 の #376 QA で実測）。作成系フック（`createScheduleItem` / `addNode`）は**楽観的 id を同期で返して裏で書く**ので、直後にリンクを撃つとリンクの INSERT が先に飛んで FK / RLS に弾かれる（どちらの writer も `auth.getUser()` の 1 往復から始まるが、リンク側にはその前置きが無い）。**「ローカル state にある」は FK 先の存在証明にならない** — これは #371 が `shared/src/utils/pendingItemLinks.ts` の冒頭に明文化済みの罠で、`DailyView.flushPendingLinks` が正しい流儀。#376 は `opts.onSaved` / `options.onSaved` で同じ規律を作成経路に通した（undo / redo には渡さない = redo が sync を再実行するため二重付与になる）
- **`shared/` に eslint config は無い**（lint は web だけの script）。`cd shared && npx eslint` は config not found で落ちるが、これは異常ではない
- **PR merge は「数分で来る」前提で動く**（2026-07-26 に 2 回 = #393 / #395、2026-07-27 に #423→#433、**2026-07-28 に #452→#453 と #454→tracker で計 6 回**）。どれも push 前に merge され、後追いのコミットが PR から漏れた。**レビュー → 修正が想定されるなら、PR を立てる前にレビューまで済ませる**。ただし #454 では自己レビューを PR 前に済ませたのに **tracker（memory / history）の更新が PR より後になって**漏れた — つまり予防策は「レビューを前倒す」だけでは足りず、**PR を立てる前に tracker 更新まで含めて全部 commit しておく**のが正しい形。漏れたら `git checkout -b <new> origin/main` + `git cherry-pick <sha>` で切り出し、**main が動いているのでゲートは必ず回し直す**。検知は `gh pr view <n> --json state,headRefOid` を push 直後に必ず撃つこと（`state=MERGED` かつ `headRefOid` が自分の HEAD と違えば置き去り確定）

- **自分の変更範囲外の型エラーの原因を「増分ビルド」と決めつけない**（2026-07-26 に一度誤診し、role-qa の監査で訂正）。`web/package.json` の build は**最初から `tsc -b --force`** で references 経由の shared をフルチェックしている（`cd web && npx tsc -b --force --dry` で確認可）。同セッションの他ブランチが緑だったのは増分のせいではなく、**分岐元の main がまだ壊れていなかった**だけ（`git merge-base origin/main <branch>` で実測できる）。#378 のような squash merge 事故は**壊れた版がどのブランチにも存在せずマージ後の main にだけ現れる**ので、手元の検証を厳しくしても捕まらない — 穴は「マージ後の main で誰もビルドしない」側にある
- **この PC には project 側 skill-lib の実体が無い**（`.claude/skills/` の中身は Mac パスへのシンボリックリンクがテキストとして checkout されている）。ただし **`task-tracker` はユーザーグローバル（`~/.claude/skills/`）にあり Skill ツールから起動できる**（2026-07-26 実測 — 「全て手動」は誤り）。`session-loader` 等プロジェクト固有スキルだけが不在
- **PR は作った直後に merge されることがある**（2026-07-26 実測）。#393 は作成から数分で merge され、後から push した Step B のコミットは PR に入らなかった。**1 PR に段階を積むつもりなら、全部 push し終えてから PR を立てる**。取り残したら `git checkout -b <new> origin/main` + `git cherry-pick <sha>` で切り出せる（Step A の内容は既に main にあるので衝突は最小 — 実際 `shared/src/index.ts` の隣接追加 1 箇所だけだった）
- ~~**`web` の lint は緑ではない**~~: `web/src/notes/NotesView.tsx:291` の main 由来 error は **2026-07-28 に解消済み**（origin/main `415cb185` で `cd web && npm run lint` が exit 0）。以後 lint 赤が出たら自分の変更を疑ってよい。なおセクションの標準ゲートは shared test / shared build / web build で lint を含まない
- **`REALTIME_TABLES`（`shared/src/context/SyncContext.tsx`）は publication と完全一致が不変式**（`shared/tests/syncRealtimeTables.test.ts` がハードカウント込みで検証）。DDL を伴わないコード削除でテーブルを購読リストから外すとテストが落ちる — #352 で一度踏んだ
- ~~**`web` にテストランナーが無い**~~ = **もう嘘**（#475 で `web` にも vitest が入り、2026-08-16 時点で 54 files / 486 pass）。`cd web && npm run test` と `npm run typecheck:tests` が回る。**ただし「判定ロジックを純関数 / フックへ寄せる」方針は今も有効**で、理由が変わった: テストランナーが無いからではなく **jsdom にレイアウトが無く `CalendarTab` のような Provider 一式 + 実座標が要る画面が載らない**から（D-20260812-refactor-2 = 画面ごと描くのが既定・載らない画面だけ切り出しで逃がす）。#976 の `useItemConversion` がこの形
